<h1 class="is-size-3 has-text-centered py-6">Search Results</h1>

<% if flights.count > 0 %>
  <%= flights.count %> result(s) found!

  <% flights.each do |flight| %>
    <br>
    <%= flight.departure_airport.code %> -> <%= flight.arrival_airport.code %> on <%= flight.local_departure_time %> for <%= flight.duration %> minutes.
  <% end %>
<% else %>
  No results.
<% end %>